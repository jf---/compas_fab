{
    "name": "ABB Controller",
    "nickname": "ABB Controller",
    "category": "COMPAS FAB",
    "subcategory": "ABB",
    "description": "Connect to an ABB controller and acquire a reusable, sticky-cached read/observe owner. Lazy: no connection opens on solve; the session connects and errors surface at the first downstream read. The credential handle is a name, never a secret.",
    "exposure": 2,
    "ghpython": {
        "isAdvancedMode": true,
        "iconDisplay": 2,
        "inputParameters": [
            {
                "name": "endpoint",
                "description": "Controller base URL. Unconnected defaults to http://127.0.0.1:80, the local RobotStudio virtual controller.",
                "typeHintID": "str"
            },
            {
                "name": "rw_version",
                "description": "RobotWare version, RW6 or RW7. Auto-creates a dropdown; defaults to RW6. Passed explicitly so RWS skips its blocking auto-detect probe.",
                "typeHintID": "str"
            },
            {
                "name": "credential_handle",
                "description": "Optional credential NAME (never a secret). Unconnected/blank uses the RobotStudio VC defaults; a named handle resolves ABB_<HANDLE>_USERNAME/PASSWORD from the environment at connect time.",
                "typeHintID": "str"
            }
        ],
        "outputParameters": [
            {
                "name": "controller",
                "description": "Reusable ControllerOwner for the READ/observe components. Not yet connected: acquisition is lazy and cached per component across solves."
            },
            {
                "name": "summary",
                "description": "Human-readable endpoint / rw_version / credential-handle-name line. Never contains a password."
            }
        ]
    }
}
